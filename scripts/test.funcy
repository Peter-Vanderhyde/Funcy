/*
Make default args in functions/unkown number of args
Maybe have all nodes save their name for debugging?
classes
lambdas
*/

func get_values() {
    return ["Peter", 23];
}

[name, age] = get_values();
print("Name: " + name + ", " + age);